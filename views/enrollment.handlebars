<h1>Enrollments Lookup</h1>

<button onclick="showEnrollmentAdd()">Add Enrollment</button>

<form id="enrollment-add" hidden>
    <h3>Add a enrollment:</h3>
    <p>Add a new enrollment to the database. All fields are required.</p> 
    <div>
        <label for="sNumber">Student ID Number:</label>
        <input type="text" id="sNumber" name="sNumber" placeholder="John" pattern="[0-9]{6}" minlength="6" maxlength="6" required>
    </div>
    <div>
        <label for="courseName">Course Name:</label>
        <input type="text" id="courseName" name="courseName" placeholder="CS344" pattern="[A-Za-z]{2}[0-9]{3}" maxlength="5" required>
    </div>
  <input type="submit" value="Add New enrollment" id="addEnrollment">
</form>

<p>Show all courses a student is taking.</p>
<form>
    <div class="enrollment-form">
        <label for="enrollment number">Student ID Number: </label>
        <input type="text" name="enrollment number" id="enrollmentnumber" required>
    </div>
    <button>Search</button>
</form>

<br>
<p>Show all courses and students professor is teaching.</p>
<form>
    <div class="professor-form">
        <label for="professor number">Professor ID Number: </label>
        <input type="text" name="professor number" id="professornumber" required>
    </div>
    <button>Search</button>
</form>

<br>
<p>Show all students taking a course.</p>
<form>
    <div class="course-form">
        <label for="course name">Course Name: </label>
        <input type="text" name="course name" id="coursename" required>
    </div>
    <button>Search</button>
</form>
<br>
<table>
    <thead>
        <tr>
            <th>Professor</th>
            <th>Course</th>
            <th>Student</th>
            <th>Room Number</th>
        </tr>
        
    </thead>
    <tbody>
        {{#each enrollments}}
        <tr>
            <td>{{professor}}</td>
            <td>{{courseName}}</td>
            <td>{{student}}</td>
            <td>{{roomNumber}}</td>
            <td id="update"><button onclick="updateEnrollment({{enrollmentID}})">Update</button></td>
            <td id="delete"><button onclick="deleteEnrollment({{enrollmentID}})">Delete</button></td>
        </tr>
        {{/each}}
    </tbody>
</table>